// Package gesture provides gesture recognizers and the gesture pipeline.
//
// Recognizers convert raw pointer events into higher-level semantic gesture
// signals. The pipeline handles routing, competition, priority, and deferred
// signal delivery.
package gesture
