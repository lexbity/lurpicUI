// Package colorscale provides color space conversions and color scale
// primitives for the scale package. It imports gfx for the Color type but
// keeps the core scale package pure (stdlib only).
package colorscale
