// Package diagnostics defines the runtime inspection and telemetry model.
//
// The package is used by the runtime, inspectors, and external tooling to
// snapshot layers, anchors, hit traces, and frame timing information without
// mutating the live engine state.
package diagnostics
