// Package runtime drives the facet tree through layout, projection, input, jobs, and rendering.
//
// The runtime owns lifecycle, dirty-state propagation, anchor reconciliation,
// frame timing, diagnostics, and platform event integration.
package runtime
