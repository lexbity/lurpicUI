// Package chart defines recipe variants and resolvers for chart marks.
//
// The package depends on theme and the shared theme/recipes slot vocabulary,
// but it must not import any marks package.
package chart
