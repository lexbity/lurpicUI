// Package voiceux provides reusable LurpicUI Voice UX marks, facets, stores,
// theme slots, and host-facing service interfaces.
//
// Voice UX renders host-owned voice state and dispatches typed commands back
// to the host. It does not own real-time audio processing.
package voiceux
