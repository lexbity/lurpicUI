[package]
name = "lurpic_render"
version = "0.1.0"
edition = "2021"
license = "MIT"
publish = false

[lib]
crate-type = ["cdylib", "staticlib"]

[profile.dev]
panic = "unwind"

[profile.release]
panic = "abort"
